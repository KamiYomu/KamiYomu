@model string

@if (string.IsNullOrWhiteSpace(Model))
{
    <span class="text-muted">Preview will appear here</span>
}
else
{
    <div class="alert alert-secondary">
        <strong>Preview:</strong> @Model
    </div>
}